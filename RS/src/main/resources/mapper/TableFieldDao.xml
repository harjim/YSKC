<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yskc.rs.dao.init.TableFieldDao">

    <select id="getTableFieldInfo" resultType="com.yskc.rs.models.excel.TableFieldInfo">
        select * from sys_tableField where companyId=#{companyId} and tableId=#{tableId}
    </select>
</mapper>